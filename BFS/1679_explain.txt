생겼던 문제점)
BFS로 전체 경우의 수 다 서치하면 될 것 같음.

Q1.근데 tree depth는 어떻게 저장하지?

->A. 덱에다 클래스나 페어드 형식으로 위치 좌표와 깊이값을 같이 저장해두면 됨.

)) 근데 메모리 에러뜸
Q.2배열 뿐 만 아니라 덱 같은 자료형에도 경우의수 있는 그대로 다 갖다 박으면 메모리 에러 뜸.

A.중복 제거, invalid 값 제거 통해서 최대한 경우의 수 줄일 수 있는대로 줄일 것.
